# Dev-only env file consumed by docker-compose.
# NOT for production: real prod secrets must come from a secret store.
# Gitignored — regenerate per checkout via `cp .env.example .env`
# and `mix phx.gen.secret`.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=camelot

DATABASE_URL="ecto://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db/${POSTGRES_DB}"
SECRET_KEY_BASE=oOC7QpyPum2f+vfgOiG/wpLLA8m3OnVjsQi67c99/OedAhY5vlO6uRvBwe1yQdYl
PHX_HOST=localhost
PORT=4000
POOL_SIZE=10
#
# Mail Stubs for CI
SMTP_RELAY=smtp.local
SMTP_PORT=25
SMTP_USERNAME=
SMTP_PASSWORD=
MAIL_FROM=noreply@camelotai.tech
MAIL_FROM_NAME="Camelot AI"
